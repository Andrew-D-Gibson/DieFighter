# General ignores
Exports/

# Rewritten from scratch on every launch by DebugLogger (res://debug_log.txt),
# so tracking it means every run shows up as a local change.
debug_log.txt


# Godot-specific ignores
.godot/
.import/
export.cfg
export_presets.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mac-specific ignores
*.DS_Store

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json
